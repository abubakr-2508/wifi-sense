# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Analysis outputs ARE tracked: the figures, CSVs and RESULTS.md are the
# deliverables of this project, and keeping them in the repository means the
# results are readable without installing anything or sourcing the recordings.
# They stay reproducible via `python analyze_csi.py`.

# Recordings are large and belong to their original authors; see README
*.csi.jsonl
*.jsonl

# Working notes kept alongside the code so they survive a move or a change of
# tooling profile, but NEVER published: they carry a roll number, an email
# address and the names of the guide, coordinator, HOD and principal. This
# repository is public. OneDrive still backs the folder up and versions it.
project-memory/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
