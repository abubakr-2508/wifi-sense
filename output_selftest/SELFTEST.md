# Self-test results

Generated by `selftest.py`. Every value below was measured by running the check named beside it; nothing here is asserted from inspection.

**14 passed, 0 failed, 1 not run.**

| ID | Test case | Requirement | Status |
|---|---|---|---|
| TC-01 | Source abstraction | Source abstraction | PASS |
| TC-02 | Modal geometry selection | Frame-geometry consistency | PASS |
| TC-03 | Pre-scan representativeness | Frame-geometry consistency | PASS |
| TC-04 | Discarded-frame recursion margin | Frame-geometry consistency | PASS |
| TC-05 | Ambient calibration | Ambient calibration | PASS |
| TC-06 | Calibration on a perfectly still input | Ambient calibration | PASS |
| TC-07 | Hysteresis | Motion decision | PASS |
| TC-08 | Debounce | Motion decision | PASS |
| TC-09 | Threshold adjustment invariant | Runtime adjustment | PASS |
| TC-10 | Refusal with a reason | Refusal with a reason | PASS |
| TC-11 | Respiration estimate | Respiration estimate | PASS |
| TC-12 | Local interface | Local interface | PASS |
| TC-13 | Network exposure | Network exposure | PASS |
| TC-14 | Offline operation, dependencies and throughput | Offline operation · Dependencies · Throughput | PASS |
| TC-15 | Analysis regression | Reproducibility | NOT RUN |

## TC-01: Source abstraction

| Field | Description |
|---|---|
| Test ID | TC-01 |
| Requirement | Source abstraction |
| Test Objective | Verify that all three sources present one interface and declare whether they have been verified against hardware |
| Test Description | Each source is instantiated and its declared interface inspected. The UDP source is then driven over loopback with a hand-built ADR-018 frame, and with a frame carrying the wrong magic number. |
| Input | Three source classes; one well-formed 20-byte ADR-018 header with a 64-subcarrier payload; one frame with magic 0xC5110002 |
| Expected Result | All three expose read/open/close/describe and a kind of rssi or csi; the ESP32 source reports verified = False; a well-formed frame decodes to 64 subcarriers; a wrong-magic frame is rejected |
| Actual Result | interface 3/3 conform; declared netsh-rssi=rssi@4Hz verified=True, recorded-csi=csi@20Hz verified=True, esp32-udp=csi@20Hz verified=False; decoded subcarriers 64; node id 1; wrong magic rejected; hardware verification NOT RUN - no ESP32-S3 board connected |
| Test Status | PASS |

## TC-02: Modal geometry selection

| Field | Description |
|---|---|
| Test ID | TC-02 |
| Requirement | Frame-geometry consistency |
| Test Objective | Verify that the loader selects the most common frame geometry rather than the first one it encounters |
| Test Description | The first decodable payload size on node 1 of the short recording is compared against the geometry the 400-frame pre-scan selects. |
| Input | pretrain recording, node 1 |
| Expected Result | The pre-scan selects 64 values, and does so in spite of the first frame carrying a different geometry |
| Actual Result | first frame 128 values; pre-scan selects 64 values; population 2133 of 2659 frames |
| Test Status | PASS |

## TC-03: Pre-scan representativeness

| Field | Description |
|---|---|
| Test ID | TC-03 |
| Requirement | Frame-geometry consistency |
| Test Objective | Verify that the geometry chosen from a 400-frame pre-scan is the same one a pass over the whole recording would choose |
| Test Description | For every node of both recordings, the pre-scan result is compared against the mode taken over all decodable frames of that node. |
| Input | Both recordings, nodes 1 and 2 |
| Expected Result | The pre-scan and the whole-file mode agree in all four cases |
| Actual Result | pretrain node 1 pre-scan 64 / whole-file 64; pretrain node 2 pre-scan 64 / whole-file 64; overnight node 1 pre-scan 64 / whole-file 64; overnight node 2 pre-scan 64 / whole-file 64; agreement 4/4 |
| Test Status | PASS |

## TC-04: Discarded-frame recursion margin

| Field | Description |
|---|---|
| Test ID | TC-04 |
| Requirement | Frame-geometry consistency |
| Test Objective | Establish how close the recorded source comes to exhausting the Python call stack, given that it recurses once per discarded frame |
| Test Description | RecordedCSISource.read() calls itself for every frame it discards (sources.py:323 and :333). The longest consecutive run of discardable frames is counted for each node of each recording and compared against the interpreter's recursion limit. |
| Input | Both recordings, nodes 1 and 2 |
| Expected Result | The longest run stays well below sys.getrecursionlimit() |
| Actual Result | pretrain node 1 2133 kept / 764 discarded, longest run 7; pretrain node 2 1994 kept / 892 discarded, longest run 7; overnight node 1 52574 kept / 19671 discarded, longest run 12; overnight node 2 54911 kept / 17820 discarded, longest run 10; worst case 12 (overnight node 1); recursion limit 1000; margin 83x |
| Test Status | PASS |

## TC-05: Ambient calibration

| Field | Description |
|---|---|
| Test ID | TC-05 |
| Requirement | Ambient calibration |
| Test Objective | Verify that the detector learns a baseline over the configured interval and reports its progress while doing so |
| Test Description | A detector is driven with seeded Gaussian samples. The state, the reported progress and the number of samples consumed before the baseline completes are recorded. |
| Input | 20 Hz, 4 s window, 15 s calibration, seeded normal(0, 1) |
| Expected Result | Progress rises monotonically to 1.0, the baseline completes after the window has filled plus the calibration interval, and no decision is reported before it does |
| Actual Result | window / calibration samples 80 / 300; baseline complete after 379 samples (18.9 s); expected 379 samples; progress monotonic True; final state idle; baseline mean 0.9287, sigma 0.0874; windows reporting calibrating 378; completing window reports idle |
| Test Status | PASS |

## TC-06: Calibration on a perfectly still input

| Field | Description |
|---|---|
| Test ID | TC-06 |
| Requirement | Ambient calibration |
| Test Objective | Verify that a zero-variance ambient period cannot produce an infinite z-score |
| Test Description | A constant signal is fed through calibration, which drives the standard deviation of window dispersion to zero. The floor at detector.py:229 should keep every subsequent z-score finite. |
| Input | 20 Hz, constant 1.0 for 600 samples |
| Expected Result | sigma_std is greater than zero and the resulting z-scores are finite |
| Actual Result | sigma_std 1.000e-06; z over 20 windows min -0.000, max -0.000; all finite True |
| Test Status | PASS |

## TC-07: Hysteresis

| Field | Description |
|---|---|
| Test ID | TC-07 |
| Requirement | Motion decision |
| Test Objective | Verify that the state entered above the upper threshold is left only below the lower one |
| Test Description | The decision rule is driven with supplied z-scores from each state, and then the whole path is exercised end to end by calibrating on quiet samples, feeding agitated ones and returning to quiet. |
| Input | enter 3.0, exit 1.5; z of 2.0 and 3.5 from idle and from motion; then seeded normal(0, 1) and normal(0, 4) |
| Expected Result | z = 2.0 does not enter the motion state but does not leave it either; the end-to-end z rises above the enter threshold under agitation and falls back below it |
| Actual Result | decision rule idle@z=2.0 -> idle; idle@z=3.5 -> motion; motion@z=2.0 -> motion; motion@z=1.0 -> idle; z quiet 0.03; z agitated 27.29 -> motion; z settled -0.42 -> idle |
| Test Status | PASS |

## TC-08: Debounce

| Field | Description |
|---|---|
| Test ID | TC-08 |
| Requirement | Motion decision |
| Test Objective | Verify that a single unusual window cannot change the reported state |
| Test Description | With the baseline already learned, a single motion-target window is applied and the state inspected, then two consecutive ones. |
| Input | debounce = 2, starting from the idle state |
| Expected Result | One window leaves the state unchanged; two consecutive windows change it |
| Actual Result | after 1 window idle; after 2 windows motion; spike, quiet, spike idle |
| Test Status | PASS |

## TC-09: Threshold adjustment invariant

| Field | Description |
|---|---|
| Test ID | TC-09 |
| Requirement | Runtime adjustment |
| Test Objective | Verify that no combination the interface can send leaves the detector with an exit threshold at or above its enter threshold, and that adjusting thresholds does not discard the learned baseline |
| Test Description | Every combination of a grid of enter and exit values, including negative, crossed and far out-of-range inputs, is applied through set_thresholds and the invariant re-checked after each. |
| Input | enter in {-5, 0.5, 2, 3, 8, 100}; exit in {-5, 0.2, 1.5, 5, 100}; debounce in {0, 2, 50} |
| Expected Result | enter > exit holds after every combination, values are clamped to their documented ranges, and the baseline is unchanged |
| Actual Result | combinations 30; invariant held 30/30; clamping enter_z(100) -> 8.0, debounce(50) -> 10; baseline preserved True |
| Test Status | PASS |

## TC-10: Refusal with a reason

| Field | Description |
|---|---|
| Test ID | TC-10 |
| Requirement | Refusal with a reason |
| Test Objective | Verify that every quantity the system cannot support is refused with a stated reason rather than returned as a number |
| Test Description | The respiration estimator is called in each of its three refusal conditions; an RSSI detector is asked for respiration; and the capability gate is inspected for a CSI source at 20 Hz and an RSSI source at 4 Hz. |
| Input | Empty array; fs = 0.8 Hz; a 5 s window at 20 Hz; source_kind rssi; both capability configurations |
| Expected Result | Each refusal carries supported = False and a distinct non-empty reason; pose is unavailable in every configuration; every capability has a reason |
| Actual Result | empty buffer "no samples"; sub-Nyquist "sample rate 0.8 Hz cannot resolve 0.5 Hz (Nyquist)"; window under one cycle "window 5.0s shorter than one cycle (10.0s)"; rssi source supported=False, "RSSI source: chest displacement perturbs subcarrier phase,..."; csi @20Hz {"motion":"validated","presence":"validated","respiration":"unvalidated","cardiac":"unvalidated","pose":"unavailable"}; rssi @4Hz {"motion":"validated","presence":"validated","respiration":"unavailable","cardiac":"unavailable","pose":"unavailable"} |
| Test Status | PASS |

## TC-11: Respiration estimate

| Field | Description |
|---|---|
| Test ID | TC-11 |
| Requirement | Respiration estimate |
| Test Objective | Verify that the estimate is the median across the highest-variance subcarriers, that structural zeros are excluded from the ranking, and that the agreement across those subcarriers is reported |
| Test Description | A synthetic 64-subcarrier stream is built with the real null pattern held at zero, six subcarriers carrying a tone placed exactly on a spectral bin, and the rest seeded noise. The stream is fed through the detector. |
| Input | 20 Hz, 620 samples, tone at 0.3125 Hz (18.75 br/min), nulls at index 0 and 27-37, noise sigma 0.2 |
| Expected Result | Eight subcarriers are chosen, none of them a null, and the returned rate is within half a spectral bin of 18.75 br/min |
| Actual Result | subcarriers chosen 8 -> [10, 11, 12, 13, 14, 15, 21, 59]; nulls among them 0; estimate 18.75 br/min (expected 18.75); agreement 0.75; bin width 4.69 br/min |
| Test Status | PASS |

## TC-12: Local interface

| Field | Description |
|---|---|
| Test ID | TC-12 |
| Requirement | Local interface |
| Test Objective | Verify that the four endpoints the browser calls respond correctly, and that the waterfall returns only the columns a client has not yet seen |
| Test Description | The real service is started on an ephemeral loopback port against the short recording. Each endpoint is called over HTTP, and the waterfall is polled twice to confirm the sequence number advances without resending. |
| Input | GET /api/state, GET /api/waterfall, POST /api/calibrate, POST /api/thresholds, GET /api/nonexistent |
| Expected Result | All four return 200 with the documented keys, an unknown route returns 404, and a second waterfall poll returns only newer columns |
| Actual Result | GET /api/state 200; GET /api/waterfall 200, 331 columns from seq 0; second poll 2 columns after seq 331; POST /api/calibrate 200, state=calibrating; POST /api/thresholds 200, {'enter_z': 4.0, 'exit_z': 2.0, 'debounce': 2}; GET /api/nonexistent 404 |
| Test Status | PASS |

## TC-13: Network exposure

| Field | Description |
|---|---|
| Test ID | TC-13 |
| Requirement | Network exposure |
| Test Objective | Verify that the server binds to the loopback address by default and refuses any request for a file outside the web directory |
| Test Description | The documented defaults of serve() and of the launcher are inspected, and path-traversal requests are sent as raw request lines through http.client so that no client-side normalisation can hide the attempt. |
| Input | GET /static/style.css, GET /static/../wifisense/detector.py, GET /static/%2e%2e/wifisense/detector.py |
| Expected Result | A legitimate static file is served; both traversal attempts are refused with 403 or 404 and no file content is returned |
| Actual Result | serve() default host 127.0.0.1; launcher default --host 127.0.0.1; legitimate file 200 (14689 bytes); traversal 403 (22 bytes); encoded traversal 404 (22 bytes) |
| Test Status | PASS |

## TC-14: Offline operation, dependencies and throughput

| Field | Description |
|---|---|
| Test ID | TC-14 |
| Requirement | Offline operation · Dependencies · Throughput |
| Test Objective | Verify that the interface fetches nothing from the network, that the server module imports only the standard library, and that the pipeline keeps pace with a 20 Hz stream on one core |
| Test Description | The three files the browser loads are scanned for references that would cause a fetch. Every module is parsed with ast and its imports classified against sys.stdlib_module_names. The detector is then driven with 2,000 synthetic CSI samples and the achieved rate measured. |
| Input | web/index.html, web/style.css, web/app.js; the five package modules; 2,000 samples of 64 subcarriers at 20 Hz |
| Expected Result | No fetchable external reference; server.py imports no third-party module; the achieved rate exceeds 20 Hz |
| Actual Result | fetchable external references 0; xml namespaces excluded 1; third-party imports __init__.py: none; detector.py: ['numpy']; dsp.py: ['numpy', 'scipy']; server.py: none; sources.py: ['numpy']; throughput 117 samples/s, slowest of 3 x 500 at steady state (range 117-119); headroom vs 20 Hz 5.8x |
| Test Status | PASS |

## TC-15: Analysis regression

| Field | Description |
|---|---|
| Test ID | TC-15 |
| Requirement | Reproducibility |
| Test Objective | Verify that regenerating the offline analysis reproduces the committed report and tables byte for byte |
| Test Description | analyze_csi.py is re-run over the short recording into a temporary directory and the SHA-256 of RESULTS.md and both CSV tables compared against the committed copies in output/. |
| Input | pretrain recording, default parameters |
| Expected Result | All three files hash identically to the committed versions |
| Actual Result | not run by default; pass --slow (takes about a minute) |
| Test Status | NOT RUN |
